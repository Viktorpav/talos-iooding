apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: iooding-critical
value: 1000000
globalDefault: false
description: "Priority class for critical iooding services (Postgres, Redis, Ingress)."
